import {connect} from 'react-redux'
import LeadDetails from '../components/LeadDetails'

const mapActionCreators = {}

const mapStateToProps = state => ({})

export default connect(mapStateToProps, mapActionCreators)(LeadDetails)
